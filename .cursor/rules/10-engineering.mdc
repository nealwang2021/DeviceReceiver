---
description: C++/Qt/CMake 工程规范
globs:
  - "**/*.h"
  - "**/*.hpp"
  - "**/*.c"
  - "**/*.cc"
  - "**/*.cpp"
  - "**/CMakeLists.txt"
  - "**/*.cmake"
---

# 架构原则

- UI、设备 I/O、持久化职责必须分离。
- 优先使用清晰的模块接口，避免隐式耦合。
- 减少全局可变状态，隔离副作用。

# 语言与框架规范

- 使用与 MSVC 2019 兼容的现代 C++（C++17 或项目基线）写法。
- 资源管理优先 RAII 和明确生命周期。
- Qt 异步边界优先使用信号槽，禁止跨线程直接访问 UI。
- 阻塞式设备/网络/文件 I/O 禁止在 UI 线程执行。

# 代码风格

- 函数应短小且职责单一。
- 命名优先可读性，除非是领域通用缩写。
- 仅在意图不明显时添加注释。
- 功能修复任务中避免大范围重构，除非为保证正确性所必需。

# 构建与验证

- 所有 CMake 改动必须兼容 Windows MSVC 2019 + Qt 5.15。
- 新行为应可验证；若已有测试基础设施，需补充或调整测试。
- 若缺少自动化测试，需记录人工验证范围与未验证项。
