---
description: DeviceReceiver 项目架构约束
alwaysApply: true
---

# 目录结构与职责

- 根目录 CMake 与构建脚本：仅负责构建编排。
- UI 模块：负责渲染、交互与视图状态管理。
- 设备通信模块：负责传输、协议解析与数据采集。
- 持久化模块：负责文件序列化与向后兼容读取。
- 测试模块（如存在）：负责集成与回归验证。

# 不可违反约束

1. 设备通信层不得依赖 UI 层。
2. 实时采集线程与 UI 渲染线程必须隔离。
3. 跨线程数据传递仅允许使用 Qt 信号槽或线程安全队列。
4. 原始数据持久化必须向后兼容，读取历史文件不得崩溃。
5. 任何阻塞 I/O 不得在主 UI 线程执行。

# 测试要求

- 任何影响协议解析、线程模型、持久化格式的改动都必须有针对性验证。
- 涉及持久化改动时，至少验证一个历史数据样本可安全加载。
- 涉及线程改动时，必须验证采集过程中 UI 仍可响应。
- 若缺少自动化测试，必须在变更说明中记录人工验证步骤。
