# DeviceReceiver 用户说明文档

本文档面向设备测试与数据分析人员，介绍 DeviceReceiver 的日常使用流程。  
如果你只想快速上手，按“安装与启动 -> 设备连接与采集 -> 实时可视化与离线回放 -> 数据导出”顺序操作即可。

## 1. 安装与启动

### 1.1 运行前准备

- 操作系统：Windows 10/11。
- 程序主构建入口：`build_cmake.bat`。
- 若你使用已打包好的程序，可直接运行 `realtime_data.exe`，无需关心源码构建细节。

### 1.2 启动方式

推荐在项目目录执行：

```cmd
build_cmake.bat
build_cmake\build\release\realtime_data.exe
```

说明：

- `build_cmake.bat` 是项目默认和推荐构建入口。
- 请确认运行的是最新构建产物，避免运行到旧目录中的历史版本。

### 1.3 首次启动后会生成的文件

程序首次启动后会在可执行文件附近生成以下文件或目录：

- `config.ini`：用户配置文件（设备参数、窗口布局、导出设置等）。
- `realtime_data.log`：运行日志文件（用于排查连接和导出问题）。
- `data/device_realtime_*.db`：会话实时数据库（用于历史总览和离线回放）。

## 2. 界面功能说明

### 2.1 主界面区域

- 主区域：多窗口绘图区（支持平铺、层叠、切换）。
- 左/侧边设备面板：设备连接、参数设置、采集控制。
- 底部“历史总览”面板：查看时间范围、切换实时/回放、导入并打开离线数据。

### 2.2 常用按钮与功能语义

- `连接`：启动设备连接与数据采集流程。
- `断开`：断开当前连接并停止采集。
- `暂停采集` / `恢复采集`：不中断连接，仅暂停或恢复数据流处理。
- `刷新`（历史总览）：刷新当前数据源的总览范围。
- `回到实时`：将浏览状态切回实时跟随。
- `导入...`：统一导入入口，支持 `.db/.csv/.h5/.hdf5`，自动识别并切到离线预览。
- `回到会话库`：从离线库返回当前会话实时库。

## 3. 设备连接与采集流程

### 3.1 标准连接步骤

1. 启动程序，确认设备面板参数。
2. 选择数据后端（例如 gRPC）并填写地址。
3. 需要无硬件调试时，启用本地模拟模式。
4. 点击 `连接`，观察状态栏与日志输出。
5. 采集期间可按需 `暂停采集` / `恢复采集`。

### 3.2 gRPC 地址与模拟模式

- DUT 与 Stage 使用独立地址配置（常见端口分别为 `50051` 与 `50052`）。
- gRPC 地址需合法（例如 `127.0.0.1:50051` 或 IPv6 格式 `[addr]:port`）。
- 当真实设备未就绪时，可启用“本地模拟”进行流程验证。

### 3.3 连接失败时建议处理

连接失败时，按以下顺序排查：

1. 检查 IP/端口是否正确、服务是否启动。
2. 检查防火墙或本机占用端口。
3. 使用模拟模式验证 UI 与导出流程是否正常。
4. 查看 `realtime_data.log` 的错误提示再做定位。

## 4. 实时可视化与离线回放

### 4.1 两种数据模式

- **会话实时模式**：读取当前运行会话生成的实时数据库，适合在线监控。
- **离线库模式**：读取导入或打开的离线数据库，适合复盘分析。

### 4.2 实时查看

- 连接成功后，绘图窗口会持续更新。
- 在 Live 状态下，时间视图会跟随最新数据。
- 长时间采集时，建议结合历史总览的时间选区进行局部分析，提升查看效率。

### 4.3 离线回放

1. 在历史总览点击 `导入...`。
2. 选择数据文件（`.db` / `.csv` / `.h5` / `.hdf5`）。
3. 通过时间刷选（brush）查看目标时间段。
4. 如需回到当前会话数据，点击 `回到会话库`。

说明：

- 选择 `.db` 时：以只读方式直接打开原始库，不会修改原文件。
- 选择 `.csv/.h5/.hdf5` 时：系统会新建 `data/import_preview_*.db` 并导入，再自动打开该新库。
- 当数据量很大时，界面可能提示采用聚合/抽样显示，这是正常表现。

## 5. 数据导出与文件格式

### 5.1 历史数据导出（从数据库）

- 入口：底部 `历史总览` 面板的 `导出…` 按钮。DB 未打开时按钮置灰。
- 导出数据源：当前打开的只读 SQLite（会话实时库 / 外部离线库），表为 `aligned_frames`。
- 支持范围：
  - `当前选区时段`：使用总览上 brush 拖出的时间框（`SelectionState`）。
  - `整个数据库`：覆盖 `[min(timestamp), max(timestamp)]`。
  - `自定义时段`：手动指定开始/结束时间。
- 支持格式：`CSV`、`HDF5`（`.h5/.hdf5`）。HDF5 未启用构建时自动灰掉，请选择 CSV。
- 执行方式：后台 Worker 线程 **流式分块** 读写，弹出进度对话框；可随时 **取消**（取消后目标文件会被删除，不保留半截数据）。
- 文件 schema（`schema_version=3`）与 `aligned_frames` 1:1 对齐：
  - 基础列：`timestamp_ms_utc, frame_sequence, detect_mode, cell_count, source_tag`
  - 40 槽位：每槽位 `amp / phase / x / y / source_channel`
  - `stage_pose/*` 列与数据集 **保留占位**（全部 0，向前兼容），当前数据库尚未记录三轴台位
  - 注：库中未单独存储 `frame_id`，文件头注释中说明

### 5.2 图像导出

- 阵列图、热力图窗口支持导出为 `PNG`。
- 常见命名示例：
  - `array_plot_yyyyMMdd_HHmmss.png`
  - `array_rgb_heatmap_yyyyMMdd_HHmmss.png`

### 5.3 默认路径与命名

- 历史数据导出默认使用 `exports` 目录，文件名形如 `db_export_yyyyMMdd_HHmmss.{csv|h5}`。
- 配置文件中的 `Export/Directory` 可覆盖默认目录。
- 建议定期整理 `exports` 目录，便于检索。

### 5.4 历史数据导入（用于离线预览）

- 入口：底部 `历史总览` 面板的 `导入…` 按钮。
- 后缀自动识别：
  - `.db`：直接只读打开该离线库。
  - `.csv/.h5/.hdf5`：导入到新建 `data/import_preview_yyyyMMdd_HHmmss.db`，并自动打开。
- 进度与取消：
  - 导入在后台 Worker 线程执行，界面弹出进度窗口，避免卡死。
  - 可随时取消；取消后会删除半成品数据库文件。

## 6. 常见问题与故障排查

### 6.1 连接失败

- 现象：点击 `连接` 后无法进入采集状态。
- 处理建议：
  - 检查地址、端口与服务状态。
  - 先用模拟模式验证程序链路。
  - 查看 `realtime_data.log` 获取具体错误信息。

### 6.2 连接成功但无数据

- 现象：状态显示已连接，但图表不更新。
- 处理建议：
  - 检查是否处于“暂停采集”状态。
  - 检查数据源是否发送有效数据。
  - 切换到模拟模式做交叉验证。

### 6.3 导出失败或无导出数据

- 现象：导出提示失败或导出文件为空。
- 处理建议：
  - 确认历史总览已连接会话库或已导入/打开离线库（`导出…` 按钮可点）。
  - 检查导出时间范围是否有效（结束时间不能早于开始时间）。
  - 若 HDF5 选项灰掉或失败，说明当前构建未启用 HDF5 支持，可改用 CSV。
  - 取消后目标文件被自动删除；若文件仍存在，通常为权限或占用导致，请手动清理。

### 6.4 历史库加载异常

- 现象：导入/打开离线库失败或总览不显示。
- 处理建议：
  - 若导入 `.db`，确认文件确实为 SQLite 数据库。
  - 若导入 `.csv/.h5/.hdf5`，确认文件来自本软件导出或字段结构兼容。
  - 检查文件是否被其他程序占用。
  - 先切回 `回到会话库`，确认程序本身链路正常后再重试。

---

## 附录：从 Markdown 导出为 Word 文档（生成 `.doc`）

本项目建议以 `用户说明文档.md` 作为维护源，再导出为 Word 版本给最终用户。

### 方案 A（推荐，使用 Word）

1. 用 Word 打开 `用户说明文档.md`。
2. 检查标题层级与段落格式。
3. 点击“另存为”并选择：
   - `用户说明文档.docx`（推荐）
   - `用户说明文档.doc`（兼容旧版）

### 方案 B（可选，使用 Pandoc）

若已安装 Pandoc，在项目目录执行：

```powershell
pandoc ".\用户说明文档.md" -o ".\用户说明文档.docx"
```

如需 `.doc`，可在 Word 中将 `.docx` 另存为 `.doc`。

---

维护约定：

- 日常只维护 `用户说明文档.md`。
- 每次对外发布前，重新导出一次 Word 版本，确保内容同步。
